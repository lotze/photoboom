<div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval=<%= Rails.env.development? ? 1000 : 5000 %>>
  <div class="carousel-inner">
    <% @photos.each_with_index do |photo, i| %>
    <div class="item<%= i==0 ? ' active' : '' %>">
      <img src="<%= photo.photo.url %>" alt="Photo <%= photo.id %>" />
      <div class="carousel-caption">
        <h3><%= photo.user.team(photo.game).name %></h3>
        <p><%= photo.mission.name %></p>
      </div>
    </div>
    <% end %>
  </div>
</div>

<!--
<div id="blueimp-gallery-carousel" class="blueimp-gallery blueimp-gallery-carousel">
  <div class="slides"></div>
  <h3 class="title"></h3>
  <a class="prev">‹</a>
  <a class="next">›</a>
  <a class="play-pause"></a>
  <ol class="indicator"></ol>
</div>

<div id="links">
  <% @photos.each do |photo| %>
  <a href="<%= photo.photo.url(:thumb) %>" title="<%= photo.mission.name %>">
    <img src="<%= photo.photo.url %>" alt="Photo <%= photo.id %>">
  </a>
  <% end %>
</div>

<script src="js/blueimp-gallery.min.js"></script>
<script>
blueimp.Gallery(
  document.getElementById('links').getElementsByTagName('a'),
  {
    container: '#blueimp-gallery-carousel',
    carousel: true,
    continuous: true,
    startSlideshow: true,
    slideshowInterval: <%= Rails.env.development? ? 1000 : 5000 %>
  }
);
</script>
-->