<h2>Team '<%= @team.name %>'</h2>

<p>Players:<ul>
  <% @team.users.each do |user| %>
  <li><%= user.name %></li>
  <% end %>
</ul></p>

<p><%= link_to 'Leave the team', leave_team_path(game_id: @team.game.id), class: 'btn btn-info' %></p>